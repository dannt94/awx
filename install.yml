---
- name: server details
  hosts: master
  remote_user: ubuntu
  become: true

  tasks:
  - name: install wget
    apt:
      name: wget
      state: present
  - name: install telnet
    apt: 
      name: telnet
      state: present

    
